---
phase: 04-core-client-ui
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
files_modified:
  - packages/client/src/components/combat/CombatLog.tsx
  - packages/client/src/components/combat/CombatLog.module.css
  - packages/client/src/components/chat/ChatPanel.tsx
  - packages/client/src/components/chat/ChatPanel.module.css
  - packages/client/src/components/combat/TeamSidebar.tsx
  - packages/client/src/components/combat/TeamSidebar.module.css
  - packages/client/src/components/combat/CombatView.tsx
  - packages/client/src/components/combat/CombatView.module.css
  - packages/client/src/app.tsx
autonomous: true
requirements:
  - UI-09
  - UI-10

must_haves:
  truths:
    - "Combat log displays recent game events (card plays, damage, status effects) in a scrollable panel"
    - "Chat panel allows typing and sending messages, displays chat history"
    - "Other players' stats (HP, energy, block) visible in a compact sidebar"
    - "Full combat layout assembles: enemies at top, shared info center, player board bottom, sidebar right"
    - "Card play flow works end-to-end: select card, click enemy target, PLAY_CARD sent to server"
  artifacts:
    - packages/client/src/components/combat/CombatLog.tsx
    - packages/client/src/components/chat/ChatPanel.tsx
    - packages/client/src/components/combat/TeamSidebar.tsx
    - packages/client/src/components/combat/CombatView.tsx
  key_links:
    - "CombatView composes EnemyZone, SharedInfo, PlayerBoard, CombatLog, ChatPanel, TeamSidebar, and CombatEnd"
    - "CombatView handles card targeting: passes selectedCard from PlayerBoard to EnemyZone, sends PLAY_CARD on enemy click"
    - "App.tsx renders CombatView when in combat phase"
    - "CombatLog reads from GameState.combatLog array"
    - "ChatPanel reads from store.chatMessages and sends SEND_CHAT messages"
---

<objective>
Assemble the complete combat view: combat log, chat panel, team sidebar, and the CombatView layout that composes all combat components. Wire up the full card play interaction flow. Update App to render CombatView.

Purpose: This is the final integration plan — all combat components come together into the playable combat screen.
Output: CombatView.tsx is the full combat layout. App.tsx renders it. Players can play cards, end turns, chat, and see the combat log.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-client-ui/04-RESEARCH.md
@.planning/phases/04-core-client-ui/04-03-SUMMARY.md
@.planning/phases/04-core-client-ui/04-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From packages/client/src/stores/gameStore.ts:
```typescript
interface AppState {
  playerId: string | null;
  reconnectionToken: string | null;
  roomCode: string | null;
  game: GameState | null;
  lobby: LobbyState | null;
  chatMessages: Array<{ playerId: string; nickname: string; text: string }>;
  error: string | null;
  phase: 'connecting' | 'lobby' | 'combat';
}
function getMyPlayer(state: AppState): PlayerState | undefined;
```

From packages/client/src/services/websocket.ts:
```typescript
function createGameConnection(url: string): {
  connect: () => void;
  send: (msg: ClientMessage) => void;
  onMessage: (handler: (msg: ServerMessage) => void) => void;
  connected: () => boolean;
};
```

<!-- From Plan 03 -->
From packages/client/src/components/combat/PlayerBoard.tsx:
```typescript
function PlayerBoard(props: {
  state: AppState;
  send: (msg: ClientMessage) => void;
  onCardSelected?: (cardId: string | null) => void;
}): JSX.Element;
```

<!-- From Plan 04 -->
From packages/client/src/components/combat/EnemyZone.tsx:
```typescript
function EnemyZone(props: {
  activeEnemies: string[];
  enemyCombatStates: Record<string, any>;
  selectedCard: string | null;
  onEnemyClick: (enemyId: string) => void;
}): JSX.Element;
```

From packages/client/src/components/combat/SharedInfo.tsx:
```typescript
function SharedInfo(props: {
  dieResult: number | null;
  phase: TurnPhase;
  round: number;
  players: Array<{ id: string; nickname: string; endedTurn: boolean }>;
}): JSX.Element;
```

From packages/client/src/components/combat/CombatEnd.tsx:
```typescript
function CombatEnd(props: {
  gameState: GameState;
  onReturnToLobby: () => void;
}): JSX.Element;
```

<!-- Shared types -->
GameState.combatLog: string[] — array of combat log entries from server
ClientMessage SEND_CHAT: { type: 'SEND_CHAT'; text: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CombatLog, ChatPanel, and TeamSidebar components</name>
  <files>packages/client/src/components/combat/CombatLog.tsx, packages/client/src/components/combat/CombatLog.module.css, packages/client/src/components/chat/ChatPanel.tsx, packages/client/src/components/chat/ChatPanel.module.css, packages/client/src/components/combat/TeamSidebar.tsx, packages/client/src/components/combat/TeamSidebar.module.css</files>
  <action>
1. Create `CombatLog.module.css`:
   - `.combatLog` — scrollable panel, max-height ~200px, dark background, border, overflow-y auto.
   - `.header` — "Combat Log" title, sticky top, dark background.
   - `.entry` — individual log entry, small text, padding, alternating subtle background tint.
   - Auto-scroll to bottom when new entries appear.

2. Create `CombatLog.tsx`:
   - Props: `{ entries: string[] }`.
   - Use `<For each={props.entries}>` to render log entries.
   - Auto-scroll to bottom: use a ref on the container, and a `createEffect` that scrolls to bottom when `props.entries.length` changes.
   - Show "No events yet" when empty.
   - Display last 50 entries maximum (slice from end).

3. Create `ChatPanel.module.css`:
   - `.chatPanel` — compact panel, max-height ~150px, dark background, border.
   - `.messages` — scrollable message list, overflow-y auto.
   - `.message` — individual message: nickname in bold + text.
   - `.inputRow` — flex row: text input + send button at bottom.
   - `.input` — dark input field, light text, flex-grow.
   - `.sendButton` — compact send button.

4. Create `ChatPanel.tsx`:
   - Props: `{ messages: Array<{ playerId: string; nickname: string; text: string }>; onSend: (text: string) => void }`.
   - Local signal for input text.
   - Send on Enter key or button click. Clear input after send.
   - Auto-scroll to bottom on new messages.
   - Use `<For each={props.messages}>` to render messages.
   - Limit to empty string check — don't send empty messages.

5. Create `TeamSidebar.module.css`:
   - `.sidebar` — narrow right sidebar, ~180px wide, dark background, vertical flex, gap between player cards.
   - `.playerCard` — compact player info card: nickname, character, HP bar, energy, block.
   - `.characterBadge` — small colored dot/badge showing character color.
   - `.endedTurn` — green checkmark next to player name.
   - `.currentPlayer` — subtle highlight for "you" in the sidebar.

6. Create `TeamSidebar.tsx`:
   - Props: `{ players: PlayerState[]; myPlayerId: string | null }`.
   - Use `<For each={props.players}>` to render compact player cards.
   - For each player: show nickname, character (colored badge), HP as small bar (current/max), energy count, block (if > 0).
   - Green ✓ checkmark next to name if `endedTurn` is true.
   - Highlight the current player's card with a subtle border.
   - Skip showing the current player in the sidebar (they have the full PlayerBoard) — OR show all players for team awareness. Show all players including self for consistency.
  </action>
  <verify>`pnpm --filter @slay-online/client typecheck` passes. All three components render with correct props.</verify>
  <done>Combat log shows game events. Chat panel sends/receives messages. Team sidebar shows all players' compact stats.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble CombatView layout and update App component</name>
  <files>packages/client/src/components/combat/CombatView.tsx, packages/client/src/components/combat/CombatView.module.css, packages/client/src/app.tsx</files>
  <action>
1. Create `CombatView.module.css`:
   - `.combatView` — full viewport height, CSS grid layout:
     ```
     grid-template-areas:
       "enemies enemies sidebar"
       "shared  shared  sidebar"
       "board   board   sidebar"
       "log     chat    sidebar"
     ```
   - `.enemies` — grid-area: enemies. Top section.
   - `.shared` — grid-area: shared. Center strip.
   - `.board` — grid-area: board. Player's board area. Flex-grow to take available space.
   - `.sidebar` — grid-area: sidebar. Right sidebar, ~200px wide.
   - `.logArea` — grid-area: log. Bottom-left.
   - `.chatArea` — grid-area: chat. Bottom-right (next to log, left of sidebar).
   - Grid columns: `1fr 1fr 200px`.
   - Grid rows: sized appropriately — enemies auto, shared auto, board 1fr (flexible), log/chat auto.
   - Dark fantasy background on the full view.

2. Create `CombatView.tsx`:
   - Props: `{ state: AppState; send: (msg: ClientMessage) => void }`.
   - Manage local `selectedCard` signal.
   - Compose all combat components in the grid layout:
     - **Enemies area:** `<EnemyZone>` with activeEnemies from game state, enemyCombatStates from game state (access as `(props.state.game as any)?.enemyCombatStates || {}`), selectedCard signal, and onEnemyClick handler.
     - **Shared area:** `<SharedInfo>` with dieResult, phase, round, and player end-turn statuses.
     - **Board area:** `<PlayerBoard>` with state and send, plus `onCardSelected` callback that updates selectedCard.
     - **Sidebar:** `<TeamSidebar>` with all players and myPlayerId.
     - **Log area:** `<CombatLog>` with game.combatLog entries.
     - **Chat area:** `<ChatPanel>` with store.chatMessages and onSend that calls `send({ type: 'SEND_CHAT', text })`.
     - **Overlay:** `<CombatEnd>` shown conditionally when phase is COMBAT_END.
   - **Card play targeting flow:**
     When `onEnemyClick(enemyId)` is called and `selectedCard` is set:
     - Call `send({ type: 'PLAY_CARD', cardId: selectedCard(), targetIds: [enemyId] })`.
     - Clear selectedCard.
     This completes the click-card-then-click-target interaction.
   - For non-targeted cards (Skills, Powers): PlayerBoard should detect when a selected card doesn't need a target and auto-play it. The PlayerBoard's onCardSelected callback can handle this: if the card type is 'Skill' or 'Power' (look up via cardLookup), send PLAY_CARD immediately without targetIds. For 'Attack' cards, keep selected and wait for enemy click.
   - Use `<Show when={props.state.game}>` to guard the entire combat view.

3. Update `packages/client/src/app.tsx`:
   - Import `CombatView` from `./components/combat/CombatView.tsx`.
   - Replace the placeholder combat div with `<CombatView state={store.state} send={connection.send} />`.
   - The lobby placeholder stays as-is (lobby UI is from Phase 3, out of scope here).
   - Add an error display: `<Show when={store.state.error}>` render error message with dismiss button (calls setError(null)).
   - Handle "Return to Lobby": The CombatEnd's onReturnToLobby should reset the store to lobby phase or reload the page. Implement as `window.location.reload()` for now (simple, functional).
  </action>
  <verify>`pnpm --filter @slay-online/client typecheck` passes. `pnpm --filter @slay-online/client build` succeeds. CombatView assembles all components. App renders CombatView in combat phase.</verify>
  <done>Full combat view assembled with all components. Card play targeting works end-to-end. App shell renders the complete combat UI.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @slay-online/client typecheck` — all types resolve
2. `pnpm --filter @slay-online/client build` — builds without errors
3. CombatView grid layout positions all components correctly
4. Card play flow: click card → highlight → click enemy → PLAY_CARD sent → card deselected
5. Non-targeted cards (Skills/Powers) auto-play on selection
6. Combat log shows game events, auto-scrolls
7. Chat panel sends/receives messages
8. Team sidebar shows all players' compact stats
9. CombatEnd overlay shows on combat end with Return to Lobby
10. App.tsx renders CombatView in combat phase
</verification>

<success_criteria>
- Complete combat layout: enemies top, shared info center, player board bottom, sidebar right, log/chat below
- Card targeting works: select card → click enemy → server receives PLAY_CARD
- Combat log scrollable, shows game events
- Chat functional: send messages, see others' messages
- Team sidebar shows other players' HP, energy, block, end-turn status
- All components compile and the full app builds successfully
- Win/loss overlay with Return to Lobby works
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-client-ui/04-05-SUMMARY.md`
</output>
