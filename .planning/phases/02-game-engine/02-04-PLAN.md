---
phase: 02-game-engine
plan: "04"
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/server/src/game/engine/deck.ts
  - packages/server/src/game/__tests__/deck.test.ts
autonomous: true
requirements:
  - CMBT-02
  - CMBT-09
  - MECH-08
  - MECH-09
  - MECH-10
  - MECH-11

must_haves:
  truths:
    - "Drawing 5 cards moves them from drawPile to hand"
    - "When drawPile has fewer cards than needed, discard pile is shuffled into drawPile first"
    - "Discarding hand moves all hand cards to discardPile except Retain cards"
    - "Ethereal cards in hand at end of turn are exhausted instead of discarded"
    - "Exhaust removes card to exhaustPile (separate from discard)"
    - "Scry reveals top X cards and allows selective discarding"
  artifacts:
    - path: "packages/server/src/game/engine/deck.ts"
      provides: "All deck manipulation functions"
      exports: ["drawCards", "discardHand", "moveToExhaust", "shuffleDiscardIntoDraw", "scry", "resolveScry"]
    - path: "packages/server/src/game/__tests__/deck.test.ts"
      provides: "Tests for all deck operations"
  key_links:
    - from: "packages/server/src/game/engine/deck.ts"
      to: "packages/server/src/game/state/combatState.ts"
      via: "reads/writes CombatPlayerState hand, drawPile, discardPile, exhaustPile"
      pattern: "CombatPlayerState"
---

<objective>
Implement all deck manipulation operations using TDD: draw, discard, shuffle, exhaust, retain, ethereal, and scry. These pure functions manage the card zones (hand, draw pile, discard pile, exhaust pile) and enforce the board game's card flow rules.

Purpose: Card zone management is used every turn (draw 5, discard hand) and by many card effects (draw, exhaust, scry). Correct handling of edge cases like empty draw pile reshuffling and ethereal/retain interactions is critical for faithful gameplay.

Output: Fully tested deck.ts module with all card zone operations.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-game-engine/02-RESEARCH.md
@.planning/phases/02-game-engine/02-01-SUMMARY.md

@packages/shared/src/schemas/cards.ts

<interfaces>
From packages/shared/src/schemas/cards.ts:
```typescript
export const PlayerCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  // ...
  exhaust: z.boolean().optional(),
  ethereal: z.boolean().optional(),
  retain: z.boolean().optional(),
  innate: z.boolean().optional(),
});
```

From packages/server/src/game/state/combatState.ts (from plan 02-01):
```typescript
// CombatPlayerState has: hand, drawPile, discardPile, exhaustPile (all string[])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD draw cards and shuffle mechanics</name>
  <files>packages/server/src/game/__tests__/deck.test.ts, packages/server/src/game/engine/deck.ts</files>
  <behavior>
    drawCards:
    - Draw 5 from pile of 10: hand gains 5, drawPile loses 5
    - Draw 5 from pile of 3 with 7 in discard: hand gains 5, discard shuffled into draw, drawPile has 5
    - Draw 5 from pile of 0 with 3 in discard: hand gains 3, drawPile empty, discardPile empty
    - Draw 0 cards: no change
    - Draw from empty draw + empty discard: no change (cannot draw)
    shuffleDiscardIntoDraw:
    - Discard pile empties, draw pile gains all cards
    - Cards are randomized (deterministic with seeded random for testing)
    - Empty discard: drawPile unchanged
  </behavior>
  <action>
  **RED phase:** Write tests in `deck.test.ts`. Create `deck.ts` with function stubs. Tests FAIL.

  **GREEN phase:**

  1. `shuffleDiscardIntoDraw(state, playerId): CombatGameState`:
     - Move all discardPile cards to drawPile
     - Shuffle the drawPile (Fisher-Yates)
     - Accept optional `rng?: () => number` parameter for deterministic testing (defaults to Math.random)
     - Return new state with updated piles

  2. `drawCards(state, playerId, count, rng?): CombatGameState`:
     - Take `count` cards from top of drawPile into hand
     - If drawPile has fewer than `count`:
       a. Take what's available from drawPile into hand
       b. Call `shuffleDiscardIntoDraw` to refill drawPile
       c. Take remaining needed cards from new drawPile
     - If still not enough cards (deck exhausted), draw what's available
     - Return new state

  All functions are pure — return new state, never mutate.
  Use `.js` extensions in imports.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- deck</automated>
  </verify>
  <done>Drawing cards works correctly including reshuffle when drawPile is empty</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD discard hand, exhaust, ethereal, retain, and scry</name>
  <files>packages/server/src/game/__tests__/deck.test.ts, packages/server/src/game/engine/deck.ts</files>
  <behavior>
    discardHand:
    - Normal cards in hand move to discardPile
    - Retain cards stay in hand
    - Ethereal cards move to exhaustPile (not discardPile)
    - Mixed hand: 2 normal, 1 retain, 1 ethereal = 2 to discard, 1 stays, 1 to exhaust
    moveToExhaust:
    - Card removed from hand, added to exhaustPile
    - Card removed from discardPile, added to exhaustPile (if target is discard)
    scry:
    - scry(state, playerId, 3) returns top 3 cards from drawPile as pending
    - resolveScry(state, playerId, discardIds) discards selected cards, keeps rest on top
    - scry(state, playerId, 5) with only 3 in draw returns 3
  </behavior>
  <action>
  **RED phase:** Add tests for discard, exhaust, ethereal, retain, scry to `deck.test.ts`. Tests FAIL.

  **GREEN phase:**

  1. `discardHand(state, playerId, cardLookup: (id: string) => { ethereal?: boolean, retain?: boolean }): CombatGameState`:
     - For each card in hand:
       - If `retain === true`: keep in hand
       - If `ethereal === true`: move to exhaustPile
       - Otherwise: move to discardPile
     - `cardLookup` function resolves card ID to its flags (caller provides this from card data)
     - Return new state

  2. `moveToExhaust(state, playerId, cardId, from: 'hand' | 'discard'): CombatGameState`:
     - Remove cardId from the specified zone
     - Add to exhaustPile
     - Return new state

  3. `scry(state, playerId, count): { state: CombatGameState, scryCards: string[] }`:
     - Peek at top `count` cards from drawPile (don't remove)
     - Return the card IDs for the player to choose from
     - Does NOT modify state yet

  4. `resolveScry(state, playerId, discardIds: string[]): CombatGameState`:
     - Remove discardIds from top of drawPile
     - Add them to discardPile
     - Remaining scried cards stay on top of drawPile in their original order
     - Return new state

  **REFACTOR phase:** Ensure all functions handle edge cases (empty hand, card not found).
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- deck</automated>
  </verify>
  <done>Ethereal cards exhaust at end of turn, Retain cards stay in hand, Scry allows selective top-deck discard, all tests pass</done>
</task>

</tasks>

<verification>
- `pnpm --filter @slay-online/server test` passes all deck tests
- Drawing with empty drawPile triggers reshuffle from discardPile
- Ethereal cards go to exhaustPile (not discardPile) when hand is discarded
- Retain cards stay in hand when hand is discarded
- Scry reveals correct number of cards and resolveScry only discards selected ones
</verification>

<success_criteria>
1. drawCards handles reshuffle from empty drawPile correctly
2. discardHand respects Retain (stays) and Ethereal (exhaust) flags
3. moveToExhaust correctly moves cards between zones
4. Scry peek + selective discard works correctly
5. All operations return new state (no mutation)
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-engine/02-04-SUMMARY.md`
</output>
