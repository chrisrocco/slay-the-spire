---
phase: 02-game-engine
plan: "07"
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - packages/server/src/game/engine/effects/resolve.ts
  - packages/server/src/game/engine/effects/registry.ts
  - packages/server/src/game/__tests__/effects.test.ts
autonomous: true
requirements:
  - CMBT-05
  - CMBT-06

must_haves:
  truths:
    - "Every effect.kind handler in resolveEffect produces correct state changes"
    - "DealDamage, GainBlock, ApplyStatus, DrawCards, Exhaust, GainEnergy all work"
    - "Effect registry has entries for all starter and common Act 1 cards"
    - "Unimplemented cards return state unchanged with a log entry"
  artifacts:
    - path: "packages/server/src/game/engine/effects/resolve.ts"
      provides: "Complete effect resolver with all handlers implemented"
      exports: ["resolveEffect", "resolveCardEffects"]
    - path: "packages/server/src/game/engine/effects/registry.ts"
      provides: "Effect entries for starter + Act 1 player cards"
      exports: ["cardEffects", "getCardEffects"]
    - path: "packages/server/src/game/__tests__/effects.test.ts"
      provides: "Tests for effect resolution"
  key_links:
    - from: "packages/server/src/game/engine/effects/resolve.ts"
      to: "packages/server/src/game/engine/damage.ts"
      via: "DealDamage handler calls applyMultiHit"
      pattern: "applyMultiHit"
    - from: "packages/server/src/game/engine/effects/resolve.ts"
      to: "packages/server/src/game/engine/deck.ts"
      via: "DrawCards/Exhaust handlers call deck functions"
      pattern: "drawCards|moveToExhaust"
    - from: "packages/server/src/game/engine/effects/resolve.ts"
      to: "packages/server/src/game/engine/status.ts"
      via: "ApplyStatus handler calls applyStatusToken"
      pattern: "applyStatusToken"
---

<objective>
Wire up the effect resolver to the damage, status, and deck systems built in earlier plans. Implement all effect.kind handlers in resolve.ts and populate the effect registry with Act 1 card effects for all four characters.

Purpose: This plan completes the bridge between card data and game mechanics. After this, `playCard` can resolve any Act 1 card's effects through the registry + resolver pipeline. The registry population is the largest single task — translating each card's text into typed effects.

Output: Working effect resolver with handlers for all effect verbs, registry populated with Act 1 card effects.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-game-engine/02-RESEARCH.md
@.planning/phases/02-game-engine/02-02-SUMMARY.md
@.planning/phases/02-game-engine/02-03-SUMMARY.md
@.planning/phases/02-game-engine/02-04-SUMMARY.md
@.planning/phases/02-game-engine/02-05-SUMMARY.md

@packages/shared/src/data/cards/ironclad.ts
@packages/shared/src/data/cards/silent.ts
@packages/shared/src/data/cards/defect.ts
@packages/shared/src/data/cards/watcher.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement all effect handlers in resolve.ts</name>
  <files>packages/server/src/game/__tests__/effects.test.ts, packages/server/src/game/engine/effects/resolve.ts</files>
  <behavior>
    - DealDamage { hits:1, amount:6, target:'chosen' } → calls applyMultiHit with 1 hit of 6 on target
    - DealDamage { hits:3, amount:2, target:'all_row' } → hits each enemy in player's row
    - GainBlock { amount:5, target:'self' } → player gains 5 block (capped at 20)
    - ApplyStatus { status:'vulnerable', amount:2, target:'chosen' } → target gains 2 vulnerable
    - DrawCards { count:2 } → player draws 2 cards
    - GainEnergy { amount:1 } → player gains 1 energy (capped at 6)
    - Exhaust { target:'self' } → card being played moves to exhaust
    - DiscardCards { count:1 } → player must discard 1 (return pending decision)
    - LoseHp { amount:3, target:'self' } → player loses 3 HP directly (ignores block)
    - Unimplemented → state unchanged, log entry added
  </behavior>
  <action>
  **RED phase:** Write tests for each effect handler. Tests FAIL.

  **GREEN phase:** Update `resolve.ts` to implement all effect handlers:

  1. Replace the TODO stubs with real implementations:
     - `DealDamage`: Call `applyMultiHit(state, 'player', context.playerId, 'enemy', context.targetId!, effect.hits, effect.amount)`. For `target: 'all_row'`, iterate enemies in same row as player. For `target: 'all'`, iterate all enemies.
     - `GainBlock`: Add block to player (or target player), cap at 20 for players.
     - `ApplyStatus`: Call `applyStatusToken(state, targetType, targetId, effect.status, effect.amount)`. Route based on `effect.target`.
     - `DrawCards`: Call `drawCards(state, context.playerId, effect.count)`.
     - `GainEnergy`: Add energy to player, cap at 6.
     - `Exhaust`: If `target: 'self'`, the current card (beingPlayed) will be moved to exhaust during cleanup. Set a flag or handle in playCard cleanup. If `target: 'chosen_hand'`, return a pending decision (for Phase 2, log as TODO).
     - `DiscardCards`: For `target: 'random'`, randomly pick and discard. For `target: 'self'`, return pending decision.
     - `Channel`: Call character-specific channel function (stub for now, plan 02-08 implements).
     - `Evoke`: Call character-specific evoke function (stub for now).
     - `EnterStance`: Call character-specific stance function (stub for now).
     - `GainShiv`: Call character-specific shiv function (stub for now).
     - `GainMiracle`: Call character-specific miracle function (stub for now).
     - `Scry`: Call deck scry function (stub for now, returns pending decision).
     - `LoseHp`: Directly reduce HP, bypass block.
     - `HealHp`: Add HP up to maxHp.
     - `GainGold`: Add gold.
     - `Conditional`: Check condition, run `then` or `else` effects.
     - `PerX`: Calculate multiplier, run effect X times.
     - `AddCardToDiscard`/`AddCardToHand`: Add card ID to respective zone.
     - `UpgradeCard`: Upgrade a card in hand (replace ID with upgraded variant).
     - `Unimplemented`: Return state unchanged, log description.

  IMPORTANT: For character-specific effects (Channel, Evoke, EnterStance, GainShiv, GainMiracle), create stubs that log "not yet implemented" — plan 02-08 will provide the real implementations. Wire them via function references that can be swapped.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- effects</automated>
  </verify>
  <done>All core effect handlers (DealDamage, GainBlock, ApplyStatus, DrawCards, GainEnergy, Exhaust, LoseHp, HealHp) work correctly with tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Populate effect registry for all Act 1 player cards</name>
  <files>packages/server/src/game/engine/effects/registry.ts</files>
  <action>
  Read each card data file and translate card text into typed CardEffect arrays:

  1. **Read all card data files** — examine the `text` field for each card:
     - `packages/shared/src/data/cards/ironclad.ts`
     - `packages/shared/src/data/cards/silent.ts`
     - `packages/shared/src/data/cards/defect.ts`
     - `packages/shared/src/data/cards/watcher.ts`

  2. **For each card** (both base and upgraded variants):
     - Parse the text field into a sequence of CardEffect objects
     - Common patterns:
       - "Deal X damage" → `{ kind: 'DealDamage', hits: 1, amount: X, target: 'chosen' }`
       - "Deal X damage Y times" → `{ kind: 'DealDamage', hits: Y, amount: X, target: 'chosen' }`
       - "Gain X Block" → `{ kind: 'GainBlock', amount: X, target: 'self' }`
       - "Apply X Vulnerable" → `{ kind: 'ApplyStatus', status: 'vulnerable', amount: X, target: 'chosen' }`
       - "Apply X Weak" → `{ kind: 'ApplyStatus', status: 'weak', amount: X, target: 'chosen' }`
       - "Apply X Poison" → `{ kind: 'ApplyStatus', status: 'poison', amount: X, target: 'chosen' }`
       - "Draw X card(s)" → `{ kind: 'DrawCards', count: X }`
       - "Gain X Energy" → `{ kind: 'GainEnergy', amount: X }`
       - "Exhaust" → `{ kind: 'Exhaust', target: 'self' }`
       - "Channel X Lightning/Frost/Dark" → `{ kind: 'Channel', orbType: '...', count: X }`
       - "Evoke X" → `{ kind: 'Evoke', count: X }`
       - "Enter Wrath/Calm" → `{ kind: 'EnterStance', stance: '...' }`
       - "Gain X Shiv(s)" → `{ kind: 'GainShiv', count: X }`

  3. **Cards with complex or conditional effects** — use `Conditional`, `PerX`, or `Unimplemented` as appropriate. It is acceptable to mark truly complex cards as `Unimplemented` with a descriptive string — correctness over coverage.

  4. **Scope:** Populate BOTH base and upgraded card variants. Use the exact card IDs from the data files.

  5. **Organize:** Group registry entries by character for readability:
     ```typescript
     // === IRONCLAD ===
     // Starters
     'strike_r': [...],
     // Commons
     ...
     // === SILENT ===
     ...
     ```

  NOTE: This is a large task (~282 cards * 2 variants). Focus on getting the common effect patterns right. Mark edge cases as Unimplemented with clear description strings.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server run typecheck</automated>
  </verify>
  <done>Effect registry has entries for all Act 1 player cards (base + upgraded), with common effects typed correctly and complex effects marked as Unimplemented where needed</done>
</task>

</tasks>

<verification>
- `pnpm --filter @slay-online/server test` passes all effect tests
- `pnpm --filter @slay-online/server run typecheck` passes
- resolveEffect handles all non-character-specific effect kinds
- Registry has entries for all card IDs found in the data files
- getCardEffects returns Unimplemented for unknown card IDs
</verification>

<success_criteria>
1. All core effect handlers produce correct state changes (DealDamage, GainBlock, ApplyStatus, etc.)
2. Effect registry covers all 282+ cards (base + upgraded) with typed effects
3. Complex/conditional cards are marked Unimplemented with descriptive strings rather than being incorrectly mapped
4. Character-specific effects have stubs ready for plan 02-08 to fill in
5. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-engine/02-07-SUMMARY.md`
</output>
