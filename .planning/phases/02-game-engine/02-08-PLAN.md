---
phase: 02-game-engine
plan: "08"
type: tdd
wave: 4
depends_on:
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - packages/server/src/game/engine/characters/ironclad.ts
  - packages/server/src/game/engine/characters/silent.ts
  - packages/server/src/game/engine/characters/defect.ts
  - packages/server/src/game/engine/characters/watcher.ts
  - packages/server/src/game/__tests__/characters.test.ts
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
  - CHAR-04

must_haves:
  truths:
    - "Ironclad: Strength tokens add +1 per hit, exhaust pile is queryable for synergy cards"
    - "Silent: Shiv tokens (max 5) each deal 1 damage as separate attacks, Poison max 30 combined"
    - "Defect: Orbs can be channeled into any slot, any orb can be evoked, forced evoke when slots full"
    - "Watcher: Entering same stance is a no-op, leaving Calm grants 2 energy, Wrath gives +1 damage but 1 end-of-turn damage"
    - "Watcher: Miracles (max 5) each grant 1 energy when used"
  artifacts:
    - path: "packages/server/src/game/engine/characters/ironclad.ts"
      provides: "Ironclad-specific mechanics"
      exports: ["ironcladStarterDeck", "getExhaustCount"]
    - path: "packages/server/src/game/engine/characters/silent.ts"
      provides: "Silent-specific mechanics: Shivs and Poison"
      exports: ["silentStarterDeck", "resolveShivs", "applyPoison"]
    - path: "packages/server/src/game/engine/characters/defect.ts"
      provides: "Defect Orb system"
      exports: ["defectStarterDeck", "channelOrb", "evokeOrb", "applyOrbEndOfTurn"]
    - path: "packages/server/src/game/engine/characters/watcher.ts"
      provides: "Watcher Stance and Miracle system"
      exports: ["watcherStarterDeck", "enterStance", "useMiracle", "applyWrathEndOfTurn"]
    - path: "packages/server/src/game/__tests__/characters.test.ts"
      provides: "Tests for all character-specific mechanics"
  key_links:
    - from: "packages/server/src/game/engine/characters/silent.ts"
      to: "packages/server/src/game/engine/damage.ts"
      via: "resolveShivs calls applyMultiHit for each shiv token"
      pattern: "applyMultiHit"
    - from: "packages/server/src/game/engine/characters/defect.ts"
      to: "packages/server/src/game/engine/damage.ts"
      via: "evokeOrb (lightning) calls applyDamage"
      pattern: "applyDamage"
    - from: "packages/server/src/game/engine/characters/defect.ts"
      to: "packages/server/src/game/engine/status.ts"
      via: "evokeOrb (frost) calls applyStatusToken for block"
      pattern: "GainBlock"
---

<objective>
Implement all four character-specific mechanics: Ironclad's strength/exhaust synergies, Silent's Shiv tokens and Poison, Defect's Orb channel/evoke/end-of-turn system, and Watcher's Stance transitions and Miracles. Each mechanic module is a set of pure functions that integrate with the damage, status, and deck systems.

Purpose: Character mechanics are what differentiate gameplay between the four characters. The board game mechanics differ from the video game in important ways (orbs don't rotate, shivs are tokens not cards, etc.) — these differences must be implemented faithfully.

Output: Four character mechanic modules with full test coverage.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-game-engine/02-RESEARCH.md
@.planning/phases/02-game-engine/02-01-SUMMARY.md
@.planning/phases/02-game-engine/02-03-SUMMARY.md
@.planning/phases/02-game-engine/02-04-SUMMARY.md

<interfaces>
From packages/server/src/game/state/combatState.ts (plan 02-01):
```typescript
// CombatPlayerState combat tokens:
shivTokens: number,        // 0-5, Silent
orbs: OrbType[],            // Defect, max slots
maxOrbSlots: number,        // Defect, typically 3
stance: 'neutral' | 'calm' | 'wrath',  // Watcher
miracleTokens: number,     // 0-5, Watcher
strengthTokens: number,    // 0-8, all characters
```

From packages/server/src/game/engine/damage.ts:
```typescript
export function applyMultiHit(state, attackerType, attackerId, targetType, targetId, hits, amount): CombatGameState;
export function applyDamage(state, targetType, targetId, rawDamage): CombatGameState;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD Ironclad and Silent character mechanics</name>
  <files>packages/server/src/game/__tests__/characters.test.ts, packages/server/src/game/engine/characters/ironclad.ts, packages/server/src/game/engine/characters/silent.ts</files>
  <behavior>
    Ironclad:
    - ironcladStarterDeck returns correct card IDs for Ironclad starter deck
    - getExhaustCount(state, playerId) returns number of cards in exhaust pile
    - Starting HP: Ironclad has highest starting HP (defined in game setup, but document the value here)
    Silent:
    - resolveShivs(state, playerId, targetId) deals 1 damage per shiv token as separate attacks
    - Each shiv is a separate Attack (affected by Strength, Vulnerable, Weak independently)
    - After all shivs resolve, shiv tokens reset to 0
    - Shiv tokens capped at 5
    - Poison application respects 30 max combined across all enemies
    - If enemy A has 20 poison and you try to apply 15 more to enemy B: only 10 applied
  </behavior>
  <action>
  **RED phase:** Write tests for Ironclad and Silent mechanics. Tests FAIL.

  **GREEN phase:**

  1. **Ironclad** (`ironclad.ts`):
     - `ironcladStarterDeck(): string[]` — return array of starter card IDs
     - `getExhaustCount(state, playerId): number` — return exhaustPile.length for player
     - Ironclad's strength mechanics are handled by the general damage formula (strengthTokens), no special code needed here

  2. **Silent** (`silent.ts`):
     - `silentStarterDeck(): string[]` — return array of starter card IDs
     - `resolveShivs(state, playerId, targetId): CombatGameState`:
       - Read `shivTokens` for player
       - For each shiv: deal 1 damage as a separate Attack (call `applyMultiHit` with hits=1, amount=1)
       - Each shiv attack is independently affected by Strength, Vulnerable, Weak
       - After all shivs: set `shivTokens` to 0
       - Return new state
     - `gainShivs(state, playerId, count): CombatGameState`:
       - Add `count` to `shivTokens`, cap at 5
       - Return new state
     - Poison functions already exist in status.ts (applyPoison with 30 max) — no additional Silent-specific code needed for poison beyond what's in status.ts

  **REFACTOR phase:** Ensure shiv resolution uses the standard damage pipeline so Strength/Vulnerable/Weak apply correctly.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- characters</automated>
  </verify>
  <done>Ironclad exhaust pile trackable, Silent shivs resolve as separate attacks with proper damage modifiers, poison 30 cap works across enemies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD Defect Orb system and Watcher Stance/Miracle system</name>
  <files>packages/server/src/game/__tests__/characters.test.ts, packages/server/src/game/engine/characters/defect.ts, packages/server/src/game/engine/characters/watcher.ts</files>
  <behavior>
    Defect Orbs:
    - channelOrb('lightning') with empty slots: orb added to orbs array
    - channelOrb('frost') with full slots (3/3): must evoke one orb first, then channel
    - evokeOrb at index 1: removes orb at index, triggers evoke effect (lightning=deal damage, frost=gain block, dark=deal damage based on dark count)
    - applyOrbEndOfTurn: Lightning orbs deal damage to random enemy, Frost orbs gain block
    - Any orb can go in any slot; any orb can be evoked (board game rule, not video game)
    Watcher Stances:
    - enterStance('wrath') from neutral: set stance to wrath
    - enterStance('wrath') from wrath: NO CHANGE (same stance is no-op)
    - enterStance('calm') from wrath: set stance to calm (no wrath bonus removed until next check)
    - enterStance('neutral') from calm: gain 2 energy (leaving Calm bonus), set stance to neutral
    - enterStance('wrath') from calm: gain 2 energy (leaving Calm), set stance to wrath
    - applyWrathEndOfTurn: if in Wrath, player takes 1 damage (direct HP loss, ignores block)
    Watcher Miracles:
    - gainMiracle(state, playerId, 1) with 3 miracles: miracles becomes 4
    - gainMiracle(state, playerId, 2) with 4 miracles: miracles becomes 5 (capped)
    - useMiracle(state, playerId): gain 1 energy, reduce miracles by 1
    - useMiracle with 0 miracles: no change
  </behavior>
  <action>
  **RED phase:** Write tests for Defect and Watcher mechanics. Tests FAIL.

  **GREEN phase:**

  1. **Defect** (`defect.ts`):
     - `defectStarterDeck(): string[]` — return array of starter card IDs
     - `channelOrb(state, playerId, orbType: OrbType): CombatGameState`:
       - If `orbs.length < maxOrbSlots`: add orbType to orbs array
       - If `orbs.length >= maxOrbSlots`: MUST evoke an orb first. For auto-resolution: evoke the first orb (index 0). In full game, player chooses which to evoke — for Phase 2, auto-evoke index 0.
       - Return new state
     - `evokeOrb(state, playerId, orbIndex: number): CombatGameState`:
       - Remove orb at orbIndex from orbs array
       - Apply evoke effect based on orb type:
         - Lightning: deal 8 damage to random enemy (use first living enemy for determinism in tests)
         - Frost: gain 5 block for the player
         - Dark: deal damage equal to number of dark orbs channeled total (simplified for Phase 2 — this may need refinement based on actual board game rules)
       - Return new state
     - `applyOrbEndOfTurn(state, playerId): CombatGameState`:
       - For each orb in player's orbs:
         - Lightning: deal 3 damage to random enemy
         - Frost: gain 2 block
         - Dark: no end-of-turn effect (gains charge, evoke to release — simplified)
       - Return new state

  2. **Watcher** (`watcher.ts`):
     - `watcherStarterDeck(): string[]` — return array of starter card IDs
     - `enterStance(state, playerId, targetStance: 'calm' | 'wrath' | 'neutral'): CombatGameState`:
       - If current stance === targetStance: return state unchanged (no-op per Pitfall 9)
       - If LEAVING Calm (current === 'calm' and target !== 'calm'): gain 2 energy
       - Set stance to targetStance
       - Return new state
     - `applyWrathEndOfTurn(state, playerId): CombatGameState`:
       - If stance === 'wrath': deal 1 damage to player (direct HP loss, bypass block)
       - Return new state
     - `gainMiracle(state, playerId, count): CombatGameState`:
       - Add count to miracleTokens, cap at 5
       - Return new state
     - `useMiracle(state, playerId): CombatGameState`:
       - If miracleTokens > 0: reduce by 1, gain 1 energy (cap at 6)
       - If 0: return unchanged
       - Return new state

  **REFACTOR phase:** Ensure all functions use the standard state update patterns (spread copy).
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- characters</automated>
  </verify>
  <done>Defect orbs channel/evoke/end-of-turn work correctly with forced evoke on full slots, Watcher stances transition correctly with Calm energy bonus and same-stance no-op, Miracles grant energy and cap at 5</done>
</task>

</tasks>

<verification>
- `pnpm --filter @slay-online/server test` passes all character tests
- Shivs deal 1 damage each as separate attacks (affected by Strength/Vulnerable/Weak)
- Orb channel with full slots forces evoke first
- Entering same stance is a no-op
- Leaving Calm grants exactly 2 energy
- Wrath end-of-turn deals 1 HP loss (bypasses block)
- Miracle grants 1 energy, capped at 5 miracles
</verification>

<success_criteria>
1. Ironclad exhaust pile is queryable; strength works via general damage formula
2. Silent shivs resolve as separate 1-damage attacks with all modifiers
3. Defect orbs: channel to any slot, evoke any orb, forced evoke when full, correct end-of-turn effects
4. Watcher: same-stance no-op, Calm exit grants 2 energy, Wrath 1 HP end-of-turn
5. All character starter decks return correct card IDs
6. All values respect their caps (shivs 5, miracles 5, orb slots)
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-engine/02-08-SUMMARY.md`
</output>
