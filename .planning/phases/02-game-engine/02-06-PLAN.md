---
phase: 02-game-engine
plan: "06"
type: tdd
wave: 3
depends_on:
  - "02-03"
files_modified:
  - packages/server/src/game/engine/enemyTurn.ts
  - packages/server/src/game/__tests__/enemyTurn.test.ts
autonomous: true
requirements:
  - CMBT-10
  - CMBT-11
  - CMBT-12
  - CMBT-13
  - CMBT-14

must_haves:
  truths:
    - "Enemies lose all block at start of enemy turn"
    - "Enemies act in order: top row left-to-right, then next row, bosses last"
    - "Single action enemies do the same action every turn"
    - "Die action enemies act based on the current die result"
    - "Cube action enemies advance position each turn; gray slots skipped on loop"
    - "Dead enemies are flipped (isDead=true) and lose all tokens"
    - "Dead player triggers game-over (unless Last Stand optional rule)"
  artifacts:
    - path: "packages/server/src/game/engine/enemyTurn.ts"
      provides: "Enemy turn resolution and AI"
      exports: ["resolveEnemyTurn", "resolveEnemyAction", "advanceCubePosition", "checkDeaths", "sortEnemiesByActionOrder"]
    - path: "packages/server/src/game/__tests__/enemyTurn.test.ts"
      provides: "Tests for enemy AI and turn resolution"
  key_links:
    - from: "packages/server/src/game/engine/enemyTurn.ts"
      to: "packages/server/src/game/engine/damage.ts"
      via: "calls applyDamage and applyMultiHit for enemy attacks"
      pattern: "applyDamage|applyMultiHit|applyHit"
    - from: "packages/server/src/game/engine/enemyTurn.ts"
      to: "packages/server/src/game/engine/status.ts"
      via: "calls applyStatusToken for enemy debuff actions"
      pattern: "applyStatusToken"
    - from: "packages/server/src/game/engine/enemyTurn.ts"
      to: "packages/server/src/game/state/enemyCombatState.ts"
      via: "reads enemy combat state for pattern resolution"
      pattern: "EnemyCombatState"
---

<objective>
Implement enemy turn resolution: block removal, action order sorting, all three action pattern types (single/die/cube), cube position advancement with gray slot skipping, and death checking. The enemy AI is deterministic given the die result and cube positions.

Purpose: Enemy AI drives the game's challenge. Correct action ordering (row-based, bosses last), cube cycling (gray slots are one-time), and death checking (immediate game-over on player death) are board game rules that must be implemented faithfully.

Output: Fully tested enemyTurn.ts module covering all enemy action patterns and death mechanics.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-game-engine/02-RESEARCH.md
@.planning/phases/02-game-engine/02-01-SUMMARY.md
@.planning/phases/02-game-engine/02-03-SUMMARY.md

@packages/shared/src/schemas/enemies.ts

<interfaces>
From packages/shared/src/schemas/enemies.ts:
```typescript
export const EnemyActionPatternSchema = z.discriminatedUnion('kind', [
  z.object({ kind: z.literal('single'), description: z.string() }),
  z.object({ kind: z.literal('die'), description: z.string(), actions: z.record(z.string(), z.string()) }),
  z.object({ kind: z.literal('cube'), description: z.string(), slots: z.array(z.object({ text: z.string(), repeating: z.boolean() })) }),
]);

export const EnemyCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  act: z.literal(1),
  category: z.enum(['encounter', 'elite', 'boss', 'summon', 'first_encounter']),
  hp: EnemyHPSchema,
  pattern: EnemyActionPatternSchema,
  specialAbilities: z.array(z.string()).default([]),
  summons: z.array(z.string()).default([]),
  rewards: z.object({ gold, cardReward, potionReward, relicReward }),
});
```

From packages/server/src/game/engine/damage.ts (plan 02-03):
```typescript
export function applyDamage(state, targetType, targetId, rawDamage): CombatGameState;
export function applyMultiHit(state, attackerType, attackerId, targetType, targetId, hits, baseAmount): CombatGameState;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD enemy action ordering and pattern resolution</name>
  <files>packages/server/src/game/__tests__/enemyTurn.test.ts, packages/server/src/game/engine/enemyTurn.ts</files>
  <behavior>
    sortEnemiesByActionOrder:
    - Enemies in row 0 act before row 1
    - Enemies in same row act left-to-right (by position within row, use array order)
    - Boss enemies (category 'boss') always act last regardless of row
    - "Acts last" enemies (from specialAbilities) act after normal enemies but before boss
    resolveEnemyAction (single):
    - Single action enemy: same action every turn (parse action text → apply effects)
    resolveEnemyAction (die):
    - Die action enemy: look up action by die result string ("1" through "6")
    - Invalid die result: no action (shouldn't happen, but handle gracefully)
    resolveEnemyAction (cube):
    - Cube action enemy: execute action at cubePosition, then advance
    advanceCubePosition:
    - Position 0 → 1 → 2 → ... → end of slots
    - After last slot: loop back to first REPEATING slot (skip gray/non-repeating slots)
    - If slots = [{text:"A",repeating:false},{text:"B",repeating:true},{text:"C",repeating:true}]:
      Turn 1: position 0 (A, gray), advance to 1
      Turn 2: position 1 (B), advance to 2
      Turn 3: position 2 (C), advance to 1 (loops to first repeating, skips gray slot 0)
    removeEnemyBlock:
    - All enemies' block set to 0
  </behavior>
  <action>
  **RED phase:** Write tests for all behaviors above. Create `enemyTurn.ts` with stubs. Tests FAIL.

  **GREEN phase:**

  1. `sortEnemiesByActionOrder(enemies: EnemyCombatState[], enemyLookup: (id: string) => EnemyCard | undefined): EnemyCombatState[]`:
     - Sort by: non-boss by row (ascending), then by position (array index preserves left-to-right)
     - "Acts last" special ability enemies sorted after normal but before boss
     - Boss always at the end
     - Return new sorted array

  2. `resolveEnemyAction(state, enemy: EnemyCombatState, enemyCard: EnemyCard, dieResult: number): CombatGameState`:
     - Switch on `enemyCard.pattern.kind`:
       - `single`: Parse and apply the single action
       - `die`: Look up action by `String(dieResult)`, parse and apply
       - `cube`: Get action at `enemy.cubePosition`, parse and apply, advance cube position
     - For Phase 2: enemy actions are parsed from description text. Create a simple parser that handles common patterns:
       - "Deal X damage" → apply X damage to player in enemy's row
       - "Deal X damage to all players" → apply X damage to all players
       - "Gain X block" → add X block to this enemy
       - "Apply X Vulnerable" → apply X vulnerable to player in row
       - "Apply X Weak" → apply X weak to player in row
       - "Apply X Strength" → add X strength to this enemy
       - For complex actions: log as unhandled and skip (to be refined)
     - Enemies target player in their row; bosses target all players

  3. `removeEnemyBlock(state): CombatGameState`:
     - Set block to 0 for all enemies in enemyCombatStates

  4. `advanceCubePosition(enemy, pattern): number`:
     - Increment cubePosition
     - If past end of slots: find first index where `repeating === true` and loop there
     - If no repeating slots exist (shouldn't happen per rules), loop to 0

  **REFACTOR phase:** Extract enemy action text parsing into a helper function for reuse.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- enemyTurn</automated>
  </verify>
  <done>Enemy ordering by row/boss, single/die/cube patterns resolve correctly, cube loops skip gray slots, enemy block reset works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD death checking and full enemy turn resolution</name>
  <files>packages/server/src/game/__tests__/enemyTurn.test.ts, packages/server/src/game/engine/enemyTurn.ts</files>
  <behavior>
    checkDeaths:
    - Enemy with HP 0: isDead set to true, all tokens cleared (vulnerable, weak, strength, poison = 0, block = 0)
    - Enemy with HP > 0: unchanged
    - Player with HP 0: state.phase = COMBAT_END, combat log records game over
    - Multiple dead enemies: all marked dead
    - Already dead enemies: unchanged (no double-processing)
    resolveEnemyTurn:
    - Removes all enemy block
    - Sorts enemies by action order
    - Resolves each living enemy's action in order
    - Checks deaths after each enemy acts (not just at end)
    - Advances cube position for cube enemies after acting
    - Sets phase to PLAYER_ACTIONS after all enemies act (start of next round)
    - If combat is over (all enemies dead), sets phase to COMBAT_END
  </behavior>
  <action>
  **RED phase:** Write death check tests and full enemy turn resolution tests. Tests FAIL.

  **GREEN phase:**

  1. `checkDeaths(state): CombatGameState`:
     - For each enemy: if hp <= 0 and not already isDead, set isDead = true and clear all tokens
     - For each player: if hp <= 0, set phase to COMBAT_END, add log entry
     - Return new state
     - Check deaths after EACH enemy acts (between actions), not just at end of turn

  2. `resolveEnemyTurn(state, enemyLookup: (id: string) => EnemyCard | undefined): CombatGameState`:
     - Call `removeEnemyBlock(state)`
     - Get living enemies (isDead === false)
     - Sort by action order
     - For each enemy in order:
       - Look up EnemyCard from enemyLookup
       - Call `resolveEnemyAction(state, enemy, enemyCard, state.dieResult)`
       - Call `checkDeaths(state)` — if any player dead or all enemies dead, short-circuit
     - After all enemies act:
       - If all enemies dead: set phase to COMBAT_END
       - Else: set phase to CLEANUP (or ready for next player turn)

  3. Add `isAllEnemiesDead(state): boolean` — check if all entries in enemyCombatStates have isDead === true

  IMPORTANT: Deaths are checked after EACH enemy action, not just at the end of the turn. If a player dies mid-turn, remaining enemies don't act.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/server test -- enemyTurn</automated>
  </verify>
  <done>Dead enemies marked correctly with tokens cleared, player death triggers game over, full enemy turn resolves in correct order with death checks between each action</done>
</task>

</tasks>

<verification>
- `pnpm --filter @slay-online/server test` passes all enemy turn tests
- Enemy action order: row 0 first, row 1 second, boss last
- Cube position loops correctly: gray slots skipped on repeat cycle
- Death check after each enemy action prevents dead enemies from acting
- Dead enemies have all tokens cleared
- Player death sets phase to COMBAT_END
</verification>

<success_criteria>
1. Enemies act in correct order: top row left-to-right, then next row, bosses last
2. Single/die/cube patterns all resolve correctly
3. Cube advances and loops to first repeating slot (skipping gray)
4. Deaths checked after each enemy action (not just end of turn)
5. Dead enemy tokens cleared, isDead set to true
6. Player death immediately ends combat
7. All enemies dead sets phase to COMBAT_END
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-engine/02-06-SUMMARY.md`
</output>
