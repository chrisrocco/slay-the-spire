---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/schemas/cards.ts
  - packages/shared/src/schemas/enemies.ts
  - packages/shared/src/schemas/gameState.ts
  - packages/shared/src/schemas/messages.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/vite.config.ts
  - packages/client/src/app.tsx
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05

must_haves:
  truths:
    - "pnpm install runs without errors from repo root"
    - "packages/client and packages/server can import from @slay-online/shared without a build step"
    - "TypeScript (tsc --noEmit) passes in all three packages"
    - "PlayerCard, EnemyCard, GameState, ClientMessage, and ServerMessage types are exported from packages/shared"
    - "Card data files (ironclad.ts, silent.ts, defect.ts, watcher.ts) and enemy data files exist as stubs ready for data entry"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package declarations"
      contains: "packages/*"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript strict settings"
      contains: "moduleResolution"
    - path: "packages/shared/src/schemas/cards.ts"
      provides: "PlayerCard, EnemyCard Zod schemas and inferred types"
      exports: ["PlayerCardSchema", "PlayerCard", "EnemyCardSchema", "EnemyCard"]
    - path: "packages/shared/src/schemas/gameState.ts"
      provides: "GameState, TurnPhase schemas"
      exports: ["GameStateSchema", "GameState"]
    - path: "packages/shared/src/schemas/messages.ts"
      provides: "WebSocket message protocol schemas"
      exports: ["ClientMessageSchema", "ClientMessage", "ServerMessageSchema", "ServerMessage"]
    - path: "packages/shared/src/schemas/enemies.ts"
      provides: "Enemy action pattern schemas (single/die/cube)"
      exports: ["EnemyActionPatternSchema", "EnemyHPSchema"]
  key_links:
    - from: "packages/server/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: "packages/shared/package.json"
      to: "packages/shared/src/index.ts"
      via: "exports field"
      pattern: "exports"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/schemas/*.ts"
      via: "barrel re-exports"
      pattern: "export \\* from"
---

<objective>
Scaffold the pnpm monorepo with three packages (shared, client, server), configure TypeScript across all packages, and define the Zod v4 schemas that will type-check all game data entered in subsequent plans.

Purpose: All downstream plans depend on the type contracts defined here. Card data files import PlayerCardSchema; enemy files import EnemyCardSchema. Without this foundation, no other work can proceed.
Output: Working monorepo with shared type contracts, stub data files for each character/enemy category, and placeholder client/server packages.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo scaffold and TypeScript configuration</name>
  <files>
    pnpm-workspace.yaml,
    package.json,
    tsconfig.base.json,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/client/package.json,
    packages/client/tsconfig.json,
    packages/client/vite.config.ts,
    packages/client/src/app.tsx,
    packages/server/package.json,
    packages/server/tsconfig.json,
    packages/server/src/index.ts
  </files>
  <action>
Create the pnpm monorepo scaffold from the project root `/home/chris/projects/slay-the-spire/`.

**pnpm-workspace.yaml:**
```yaml
packages:
  - 'packages/*'
```

**package.json (root):**
```json
{
  "name": "slay-the-spire-online",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter './packages/*' dev",
    "build": "pnpm --recursive --filter './packages/*' build",
    "test": "pnpm --recursive --filter './packages/*' test",
    "typecheck": "pnpm --recursive --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
```

**tsconfig.base.json (root):**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

**packages/shared/package.json:**
```json
{
  "name": "@slay-online/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
```

**packages/shared/tsconfig.json:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

**packages/client/package.json:**
```json
{
  "name": "@slay-online/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@slay-online/shared": "workspace:*",
    "solid-js": "^1.9.11",
    "@solid-primitives/websocket": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^4.0.0",
    "@solidjs/testing-library": "latest",
    "jsdom": "latest"
  }
}
```

**packages/client/tsconfig.json:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

**packages/client/vite.config.ts:**
```typescript
import { defineConfig } from 'vite';
import solidPlugin from 'vite-plugin-solid';

export default defineConfig({
  plugins: [solidPlugin()],
  server: {
    port: 3000,
  },
  build: {
    target: 'esnext',
  },
});
```

**packages/client/src/app.tsx** — minimal placeholder:
```typescript
// Placeholder for Phase 4 — SolidJS UI implementation
export function App() {
  return <div>Slay the Spire Online — Coming in Phase 4</div>;
}
```

**packages/server/package.json:**
```json
{
  "name": "@slay-online/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@slay-online/shared": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "tsx": "latest",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.0",
    "vitest": "^4.0.0"
  }
}
```

**packages/server/tsconfig.json:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "types": ["node"]
  },
  "include": ["src/**/*"]
}
```

**packages/server/src/index.ts** — placeholder:
```typescript
// Placeholder for Phase 3 — WebSocket server implementation
console.log('Slay the Spire Online — server placeholder');
```

After creating all files, run from the project root:
```bash
pnpm install
```

This installs all workspace dependencies and creates the symlinks between packages.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm install && echo "INSTALL OK"</automated>
  </verify>
  <done>pnpm install completes without errors; node_modules/.pnpm exists; packages/client/node_modules/@slay-online/shared symlink resolves to packages/shared/src/index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Zod v4 schemas and shared data stubs</name>
  <files>
    packages/shared/src/schemas/cards.ts,
    packages/shared/src/schemas/enemies.ts,
    packages/shared/src/schemas/gameState.ts,
    packages/shared/src/schemas/messages.ts,
    packages/shared/src/data/cards/ironclad.ts,
    packages/shared/src/data/cards/silent.ts,
    packages/shared/src/data/cards/defect.ts,
    packages/shared/src/data/cards/watcher.ts,
    packages/shared/src/data/enemies/encounters.ts,
    packages/shared/src/data/enemies/elites.ts,
    packages/shared/src/data/enemies/bosses.ts,
    packages/shared/src/data/events.ts,
    packages/shared/src/data/curses.ts,
    packages/shared/src/data/statuses.ts,
    packages/shared/src/data/relics.ts,
    packages/shared/src/data/potions.ts,
    packages/shared/src/index.ts
  </files>
  <action>
Create all Zod v4 schemas and stub data files. Use ONLY Zod v4 API — no v3 patterns.

**packages/shared/src/schemas/cards.ts:**
```typescript
import { z } from 'zod';

export const CharacterSchema = z.enum(['ironclad', 'silent', 'defect', 'watcher']);
export type Character = z.infer<typeof CharacterSchema>;

export const CardTypeSchema = z.enum(['Attack', 'Skill', 'Power', 'Curse', 'Status', 'Daze']);
export type CardType = z.infer<typeof CardTypeSchema>;

export const RaritySchema = z.enum(['starter', 'common', 'uncommon', 'rare', 'colorless', 'special']);
export type Rarity = z.infer<typeof RaritySchema>;

export const PlayerCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  character: CharacterSchema.or(z.literal('colorless')),
  rarity: RaritySchema,
  type: CardTypeSchema,
  cost: z.number().int().min(0).max(6).or(z.literal('X')).or(z.literal('unplayable')),
  text: z.string(),
  upgraded: z.boolean().default(false),
  keywords: z.array(z.string()).default([]),
  // Effects typed in Phase 2 — Phase 1 stores raw text only
  exhaust: z.boolean().optional(),
  ethereal: z.boolean().optional(),
  retain: z.boolean().optional(),
  innate: z.boolean().optional(),
});
export type PlayerCard = z.infer<typeof PlayerCardSchema>;

export const CurseCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  type: z.literal('Curse'),
  cost: z.literal('unplayable'),
  text: z.string(),
});
export type CurseCard = z.infer<typeof CurseCardSchema>;

export const StatusCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  type: z.literal('Status'),
  variant: z.enum(['wound', 'slimed']),
  cost: z.literal('unplayable'),
  text: z.string(),
});
export type StatusCard = z.infer<typeof StatusCardSchema>;

export const DazeCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  type: z.literal('Daze'),
  cost: z.literal('unplayable'),
  text: z.string(),
});
export type DazeCard = z.infer<typeof DazeCardSchema>;
```

**packages/shared/src/schemas/enemies.ts:**
```typescript
import { z } from 'zod';

// HP is either a fixed number or scales with player count (see rulebook p.11)
export const EnemyHPSchema = z.union([
  z.number().int().positive(),
  z.object({
    1: z.number().int().positive(),
    2: z.number().int().positive(),
    3: z.number().int().positive(),
    4: z.number().int().positive(),
  }),
]);
export type EnemyHP = z.infer<typeof EnemyHPSchema>;

// Phase 1: store action text only. Phase 2 adds typed effects.
export const EnemyActionPatternSchema = z.discriminatedUnion('kind', [
  z.object({
    kind: z.literal('single'),
    description: z.string(),
  }),
  z.object({
    kind: z.literal('die'),
    description: z.string(),
    // Map die result as string key ("1"-"6") to action description
    actions: z.record(z.string(), z.string()),
  }),
  z.object({
    kind: z.literal('cube'),
    description: z.string(),
    slots: z.array(z.object({
      text: z.string(),
      repeating: z.boolean(), // false = gray slot, not repeated when cube loops
    })),
  }),
]);
export type EnemyActionPattern = z.infer<typeof EnemyActionPatternSchema>;

export const EnemyCardSchema = z.object({
  id: z.string(),
  name: z.string(),
  act: z.literal(1),
  category: z.enum(['encounter', 'elite', 'boss', 'summon', 'first_encounter']),
  hp: EnemyHPSchema,
  pattern: EnemyActionPatternSchema,
  specialAbilities: z.array(z.string()).default([]),
  summons: z.array(z.string()).default([]), // enemy IDs summoned at combat start
  rewards: z.object({
    gold: z.number().int().min(0).default(0),
    cardReward: z.boolean().default(false),
    potionReward: z.boolean().default(false),
    relicReward: z.boolean().default(false),
  }),
});
export type EnemyCard = z.infer<typeof EnemyCardSchema>;
```

**packages/shared/src/schemas/gameState.ts:**
```typescript
import { z } from 'zod';
import { CharacterSchema } from './cards.js';

export const TurnPhaseSchema = z.enum([
  'PLAYER_ACTIONS',
  'WAITING_FOR_ALL_PLAYERS',
  'ENEMY_TURN',
  'CLEANUP',
  'COMBAT_END',
]);
export type TurnPhase = z.infer<typeof TurnPhaseSchema>;

export const PlayerStateSchema = z.object({
  id: z.string(),
  nickname: z.string(),
  character: CharacterSchema,
  hp: z.number().int().min(0),
  maxHp: z.number().int().positive(),
  block: z.number().int().min(0).max(20),
  energy: z.number().int().min(0).max(3),
  gold: z.number().int().min(0),
  hand: z.array(z.string()),         // card IDs
  drawPile: z.array(z.string()),
  discardPile: z.array(z.string()),
  exhaustPile: z.array(z.string()),
  relics: z.array(z.string()),       // relic IDs
  potions: z.array(z.string()),      // potion IDs, max 3
  endedTurn: z.boolean(),
});
export type PlayerState = z.infer<typeof PlayerStateSchema>;

export const GameStateSchema = z.object({
  roomCode: z.string(),
  phase: TurnPhaseSchema,
  round: z.number().int().min(1),
  dieResult: z.number().int().min(1).max(6).nullable(),
  players: z.array(PlayerStateSchema),
  activeEnemies: z.array(z.string()), // enemy card IDs on the field
  combatLog: z.array(z.string()),
});
export type GameState = z.infer<typeof GameStateSchema>;

export const LobbyStateSchema = z.object({
  roomCode: z.string(),
  players: z.array(z.object({
    id: z.string(),
    nickname: z.string(),
    character: CharacterSchema.nullable(),
    isHost: z.boolean(),
  })),
  optionalRules: z.object({
    lastStand: z.boolean(),
    chooseYourRelic: z.boolean(),
  }),
  started: z.boolean(),
});
export type LobbyState = z.infer<typeof LobbyStateSchema>;
```

**packages/shared/src/schemas/messages.ts:**
```typescript
import { z } from 'zod';
import { GameStateSchema, LobbyStateSchema } from './gameState.js';

// Client → Server intent messages (players send intents only; server resolves all logic)
export const ClientMessageSchema = z.discriminatedUnion('type', [
  z.object({ type: z.literal('PLAY_CARD'), cardId: z.string(), targetIds: z.array(z.string()).optional() }),
  z.object({ type: z.literal('END_TURN') }),
  z.object({ type: z.literal('USE_POTION'), potionId: z.string(), targetId: z.string().optional() }),
  z.object({ type: z.literal('SEND_CHAT'), text: z.string().max(500) }),
  z.object({ type: z.literal('JOIN_LOBBY'), roomCode: z.string(), nickname: z.string() }),
  z.object({ type: z.literal('SELECT_CHARACTER'), character: z.string() }),
  z.object({ type: z.literal('START_GAME') }),
]);
export type ClientMessage = z.infer<typeof ClientMessageSchema>;

// Server → Client messages
export const ServerMessageSchema = z.discriminatedUnion('type', [
  z.object({ type: z.literal('STATE_UPDATE'), state: GameStateSchema }),
  z.object({ type: z.literal('LOBBY_UPDATE'), lobby: LobbyStateSchema }),
  z.object({ type: z.literal('ERROR'), code: z.string(), message: z.string() }),
  z.object({ type: z.literal('CHAT_MESSAGE'), playerId: z.string(), text: z.string() }),
]);
export type ServerMessage = z.infer<typeof ServerMessageSchema>;
```

**Create stub data files** — these will be filled in by Plans 02-07. Each stub exports an empty array and an empty map so imports resolve without errors:

**packages/shared/src/data/cards/ironclad.ts:**
```typescript
import type { PlayerCard } from '../../schemas/cards.js';

// TODO: Filled in by Plan 02 — data extracted from ironclad_main.webp, ironclad_main_upgraded.webp, ironclad_rares.webp, ironclad_starters.webp
export const ironcladCards: readonly PlayerCard[] = [];

export const ironcladCardMap: Record<string, PlayerCard> = Object.fromEntries(
  ironcladCards.map(c => [c.id, c])
);
```

Create the same stub pattern for:
- `packages/shared/src/data/cards/silent.ts` (TODO: Plan 03)
- `packages/shared/src/data/cards/defect.ts` (TODO: Plan 04)
- `packages/shared/src/data/cards/watcher.ts` (TODO: Plan 05)
- `packages/shared/src/data/enemies/encounters.ts` (TODO: Plan 06, export `encounterEnemies: readonly EnemyCard[]`, `encounterEnemyMap: Record<string, EnemyCard>`)
- `packages/shared/src/data/enemies/elites.ts` (TODO: Plan 06, same pattern)
- `packages/shared/src/data/enemies/bosses.ts` (TODO: Plan 06, same pattern)
- `packages/shared/src/data/events.ts` (TODO: Plan 07)
- `packages/shared/src/data/curses.ts` (TODO: Plan 07)
- `packages/shared/src/data/statuses.ts` (TODO: Plan 07)
- `packages/shared/src/data/relics.ts` (TODO: Plan 07)
- `packages/shared/src/data/potions.ts` (TODO: Plan 07)

For event/curse/status/relic/potion stubs, define minimal types inline or import from a schemas file:
```typescript
// events.ts stub example
export interface EventChoice { text: string; effect: string; }
export interface EventCard { id: string; name: string; text: string; choices: EventChoice[]; }
export const events: readonly EventCard[] = [];
```

```typescript
// relics.ts stub example
export interface RelicCard { id: string; name: string; text: string; category: 'common' | 'uncommon' | 'rare' | 'boss' | 'special'; }
export const relics: readonly RelicCard[] = [];
export const relicMap: Record<string, RelicCard> = {};
```

```typescript
// potions.ts stub example
export interface PotionCard { id: string; name: string; text: string; }
export const potions: readonly PotionCard[] = [];
export const potionMap: Record<string, PotionCard> = {};
```

**packages/shared/src/index.ts** — barrel export:
```typescript
// Schemas and types
export * from './schemas/cards.js';
export * from './schemas/enemies.js';
export * from './schemas/gameState.js';
export * from './schemas/messages.js';

// Static game data (stubs — populated in Plans 02-07)
export * from './data/cards/ironclad.js';
export * from './data/cards/silent.js';
export * from './data/cards/defect.js';
export * from './data/cards/watcher.js';
export * from './data/enemies/encounters.js';
export * from './data/enemies/elites.js';
export * from './data/enemies/bosses.js';
export * from './data/events.js';
export * from './data/curses.js';
export * from './data/statuses.js';
export * from './data/relics.js';
export * from './data/potions.js';
```

Note: `.js` extensions in imports are required for ESM with `moduleResolution: bundler` — TypeScript convention even for `.ts` source files.

After creating all files, run typecheck:
```bash
cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/shared typecheck
```
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && pnpm --filter @slay-online/shared typecheck && echo "TYPECHECK OK"</automated>
  </verify>
  <done>
    - All schema files exist and export their types
    - All stub data files exist and export empty arrays/maps
    - packages/shared typechecks without errors
    - packages/server and packages/client can import from @slay-online/shared (verified by running typecheck on all)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full monorepo typechecks:

```bash
cd /home/chris/projects/slay-the-spire && pnpm typecheck
```

All three packages should pass tsc --noEmit. Confirm @slay-online/shared resolves from packages/server and packages/client by checking that `import type { PlayerCard } from '@slay-online/shared'` compiles successfully.
</verification>

<success_criteria>
- pnpm install runs without errors
- pnpm typecheck passes for all three packages (shared, client, server)
- packages/shared/src/schemas/ contains cards.ts, enemies.ts, gameState.ts, messages.ts
- packages/shared/src/data/ contains stubs for all 12 data files (4 character card files, 3 enemy files, 5 misc files)
- PlayerCard, EnemyCard, GameState, ClientMessage, ServerMessage types all exported from @slay-online/shared
- packages/client and packages/server have @slay-online/shared as a workspace dependency
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Files created (list all files)
- Key schema exports (PlayerCard, EnemyCard, GameState, ClientMessage, ServerMessage field shapes)
- Any deviations from the plan and why
- Typecheck result confirmation
</output>
