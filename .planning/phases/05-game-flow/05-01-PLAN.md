---
phase: 05-game-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/gameState.ts
  - packages/shared/src/schemas/messages.ts
  - packages/shared/src/index.ts
  - packages/server/src/game/state/combatState.ts
autonomous: true
requirements: [MAP-01, MAP-03, RWRD-06]
must_haves:
  truths:
    - GameState includes a gamePhase field distinguishing MAP, EVENT, CAMPFIRE, TREASURE, MERCHANT, COMBAT, REWARDS, BOSS_REWARD phases
    - GameState includes map data (nodes, connections, currentNodeId) so clients can render the map
    - GameState includes rewardState and roomContext fields for non-combat phases
    - All new ClientMessage types for Phase 5 are defined and validated by Zod
    - CombatGameState extends GameState seamlessly (no breaking changes to existing combat flow)
  artifacts:
    - packages/shared/src/schemas/gameState.ts
    - packages/shared/src/schemas/messages.ts
  key_links:
    - GameStateSchema is the source of truth for all state broadcast to clients
    - ClientMessageSchema validates all player intent messages
---

<objective>
Extend GameState and message schemas for the full game flow.

Purpose: The current GameState only models combat. Phase 5 needs map navigation, events, campfires, merchants, treasure, and rewards — all requiring new state fields and message types.
Output: Extended GameStateSchema with gamePhase, map, rewardState, roomContext fields; extended ClientMessageSchema with SELECT_NODE, EVENT_CHOICE, CAMPFIRE_CHOICE, MERCHANT_BUY, MERCHANT_REMOVE_CARD, REWARD_PICK_CARD, REWARD_PICK_POTION, REWARD_SKIP, PASS_POTION, DISCARD_POTION messages.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-flow/05-RESEARCH.md

<interfaces>
<!-- Current GameState and messages the executor must extend -->

From packages/shared/src/schemas/gameState.ts:
```typescript
export const TurnPhaseSchema = z.enum(['PLAYER_ACTIONS', 'WAITING_FOR_ALL_PLAYERS', 'ENEMY_TURN', 'CLEANUP', 'COMBAT_END']);
export type TurnPhase = z.infer<typeof TurnPhaseSchema>;

export const PlayerStateSchema = z.object({
  id: z.string(), nickname: z.string(), character: CharacterSchema,
  hp: z.number().int().min(0), maxHp: z.number().int().positive(),
  block: z.number().int().min(0).max(20), energy: z.number().int().min(0).max(6),
  gold: z.number().int().min(0), hand: z.array(z.string()),
  drawPile: z.array(z.string()), discardPile: z.array(z.string()),
  exhaustPile: z.array(z.string()), relics: z.array(z.string()),
  potions: z.array(z.string()), endedTurn: z.boolean(),
});

export const GameStateSchema = z.object({
  roomCode: z.string(), phase: TurnPhaseSchema,
  round: z.number().int().min(1), dieResult: z.number().int().min(1).max(6).nullable(),
  players: z.array(PlayerStateSchema), activeEnemies: z.array(z.string()),
  combatLog: z.array(z.string()),
});
```

From packages/shared/src/schemas/messages.ts:
```typescript
export const ClientMessageSchema = z.discriminatedUnion('type', [
  z.object({ type: z.literal('CREATE_ROOM'), nickname: z.string() }),
  z.object({ type: z.literal('JOIN_LOBBY'), roomCode: z.string(), nickname: z.string() }),
  z.object({ type: z.literal('SELECT_CHARACTER'), character: z.string() }),
  z.object({ type: z.literal('TOGGLE_RULE'), rule: z.enum(['lastStand', 'chooseYourRelic']) }),
  z.object({ type: z.literal('START_GAME') }),
  z.object({ type: z.literal('PLAY_CARD'), cardId: z.string(), targetIds: z.array(z.string()).optional() }),
  z.object({ type: z.literal('END_TURN') }),
  z.object({ type: z.literal('USE_POTION'), potionId: z.string(), targetId: z.string().optional() }),
  z.object({ type: z.literal('SEND_CHAT'), text: z.string().max(500) }),
  z.object({ type: z.literal('RECONNECT'), roomCode: z.string(), token: z.string() }),
]);
```

From packages/server/src/lobby/mapGenerator.ts:
```typescript
export type RoomType = 'encounter' | 'elite' | 'event' | 'campfire' | 'treasure' | 'merchant' | 'boss';
export interface MapNode { id: string; floor: number; type: RoomType; connections: string[]; }
export interface GameMap { nodes: MapNode[]; bossNodeId: string; currentNodeId: string | null; }
```

From packages/server/src/game/state/combatState.ts:
```typescript
export const CombatGameStateSchema = GameStateSchema.extend({
  players: z.array(CombatPlayerStateSchema),
  enemyCombatStates: z.record(z.string(), EnemyCombatStateSchema),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GameState schema with game flow fields</name>
  <files>packages/shared/src/schemas/gameState.ts, packages/shared/src/index.ts</files>
  <action>
Extend the GameState schema to support full game flow. Keep backward compatibility with existing combat state.

1. Add a `GamePhaseSchema` enum: `'MAP' | 'EVENT' | 'CAMPFIRE' | 'TREASURE' | 'MERCHANT' | 'COMBAT' | 'REWARDS' | 'BOSS_REWARD'`

2. Add `MapNodeSchema` and `GameMapSchema` Zod schemas (mirroring the TypeScript interfaces in mapGenerator.ts):
   ```
   MapNodeSchema = z.object({ id: z.string(), floor: z.number(), type: RoomTypeSchema, connections: z.array(z.string()) })
   RoomTypeSchema = z.enum(['encounter','elite','event','campfire','treasure','merchant','boss'])
   GameMapSchema = z.object({ nodes: z.array(MapNodeSchema), bossNodeId: z.string(), currentNodeId: z.string().nullable() })
   ```

3. Add `RewardStateSchema`:
   ```
   CardRewardSchema = z.object({ cardIds: z.array(z.string()), upgraded: z.boolean().default(false) })
   RewardStateSchema = z.object({
     gold: z.number().int().min(0).default(0),
     cardRewards: z.array(CardRewardSchema).default([]),  // one per player
     potionReward: z.string().nullable().default(null),    // potion ID or null
     relicReward: z.string().nullable().default(null),     // relic ID or null
     playerChoices: z.record(z.string(), z.object({        // keyed by playerId
       cardPicked: z.string().nullable().default(null),
       potionPicked: z.boolean().default(false),
       relicPicked: z.boolean().default(false),
       skipped: z.boolean().default(false),
     })).default({}),
   })
   ```

4. Add `EventStateSchema`:
   ```
   EventStateSchema = z.object({
     eventId: z.string(),
     playerChoices: z.record(z.string(), z.number().nullable()).default({}),  // playerId -> choice index or null
   })
   ```

5. Add `MerchantStateSchema`:
   ```
   MerchantStateSchema = z.object({
     cardPool: z.array(z.object({ cardId: z.string(), price: z.number() })),
     relicPool: z.array(z.object({ relicId: z.string(), price: z.number() })),
     potionPool: z.array(z.object({ potionId: z.string(), price: z.number() })),
     removeCost: z.number().int().default(50),
     playersRemoved: z.array(z.string()).default([]),  // playerIds who already removed
   })
   ```

6. Add `CampfireChoiceSchema`:
   ```
   CampfireChoiceSchema = z.object({
     playerChoices: z.record(z.string(), z.enum(['rest', 'smith', 'dig', 'lift', 'toke']).nullable()).default({}),
   })
   ```

7. Extend `GameStateSchema` with optional fields (so existing combat-only state is still valid):
   ```
   gamePhase: GamePhaseSchema.default('COMBAT'),
   map: GameMapSchema.optional(),
   currentFloor: z.number().int().min(0).default(0),
   eventState: EventStateSchema.optional(),
   rewardState: RewardStateSchema.optional(),
   merchantState: MerchantStateSchema.optional(),
   campfireState: CampfireChoiceSchema.optional(),
   ```

8. Export all new types from index.ts.

CRITICAL: Use `.optional()` or `.default()` for all new fields so existing code that creates GameState objects without these fields continues to work. The `phase` field (TurnPhaseSchema) remains for combat phase tracking; `gamePhase` (GamePhaseSchema) is the top-level game loop phase.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>GameState schema extended with gamePhase, map, rewardState, eventState, merchantState, campfireState fields. All new schemas exported. TypeScript compiles without errors. Existing combat state creation code is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 5 client message types</name>
  <files>packages/shared/src/schemas/messages.ts</files>
  <action>
Add new ClientMessage types for all Phase 5 interactions to the discriminated union:

```typescript
// Map navigation
z.object({ type: z.literal('SELECT_NODE'), nodeId: z.string() }),

// Event room
z.object({ type: z.literal('EVENT_CHOICE'), choiceIndex: z.number().int().min(0) }),

// Campfire
z.object({ type: z.literal('CAMPFIRE_CHOICE'), choice: z.enum(['rest', 'smith', 'dig', 'lift', 'toke']), cardId: z.string().optional() }),

// Merchant
z.object({ type: z.literal('MERCHANT_BUY'), itemType: z.enum(['card', 'relic', 'potion']), itemId: z.string() }),
z.object({ type: z.literal('MERCHANT_REMOVE_CARD'), cardId: z.string() }),
z.object({ type: z.literal('MERCHANT_LEAVE') }),

// Rewards
z.object({ type: z.literal('REWARD_PICK_CARD'), cardId: z.string() }),
z.object({ type: z.literal('REWARD_PICK_POTION') }),
z.object({ type: z.literal('REWARD_PICK_RELIC') }),
z.object({ type: z.literal('REWARD_SKIP') }),

// Potion management
z.object({ type: z.literal('PASS_POTION'), potionId: z.string(), targetPlayerId: z.string() }),
z.object({ type: z.literal('DISCARD_POTION'), potionId: z.string() }),
```

Keep all existing message types unchanged.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All new client message types added to ClientMessageSchema discriminated union. Existing message types unchanged. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Update CombatGameState to extend new GameState</name>
  <files>packages/server/src/game/state/combatState.ts</files>
  <action>
Verify CombatGameState still extends GameState correctly after the schema changes. The `CombatGameStateSchema = GameStateSchema.extend({...})` pattern should pick up the new optional fields automatically.

If there are any type conflicts between the new optional fields and combat-specific needs, resolve them. Specifically:
- `gamePhase` should default to `'COMBAT'` in combat contexts
- The existing `phase` field (TurnPhaseSchema) should continue to work as the combat turn phase

Run the TypeScript compiler to confirm no regressions. If the existing test suite references GameState, run tests to verify:
```
cd /home/chris/projects/slay-the-spire && npx vitest run --reporter=verbose 2>&1 | tail -20
```

Fix any type errors that arise from the schema extension.
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>CombatGameState extends updated GameState without conflicts. All existing tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` succeeds from project root
2. `npx vitest run` passes all existing tests
3. GameState type includes gamePhase, map, rewardState, eventState, merchantState, campfireState
4. ClientMessage type includes SELECT_NODE, EVENT_CHOICE, CAMPFIRE_CHOICE, MERCHANT_BUY, MERCHANT_REMOVE_CARD, MERCHANT_LEAVE, REWARD_PICK_CARD, REWARD_PICK_POTION, REWARD_PICK_RELIC, REWARD_SKIP, PASS_POTION, DISCARD_POTION
</verification>

<success_criteria>
All Phase 5 schemas and message types are defined. Existing combat flow is unbroken. The foundation is laid for all subsequent plans to implement handlers and UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-flow/05-01-SUMMARY.md`
</output>
