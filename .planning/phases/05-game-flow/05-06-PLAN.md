---
phase: 05-game-flow
plan: 06
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - packages/client/src/components/map/MapView.tsx
  - packages/client/src/components/map/MapView.module.css
  - packages/client/src/components/map/MapNode.tsx
  - packages/client/src/components/map/MapNode.module.css
  - packages/client/src/components/rooms/EventView.tsx
  - packages/client/src/components/rooms/EventView.module.css
  - packages/client/src/components/rooms/CampfireView.tsx
  - packages/client/src/components/rooms/CampfireView.module.css
  - packages/client/src/components/rooms/TreasureView.tsx
  - packages/client/src/components/rooms/TreasureView.module.css
  - packages/client/src/components/rooms/MerchantView.tsx
  - packages/client/src/components/rooms/MerchantView.module.css
  - packages/client/src/stores/gameStore.ts
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, ROOM-04, ROOM-05, ROOM-06, ROOM-07]
must_haves:
  truths:
    - Map displays all 15 floors vertically (bottom to top) with room type icons
    - Current position shows a boot meeple indicator
    - Host can click available next nodes to navigate; other players see read-only
    - Available nodes are highlighted; unavailable are dimmed
    - Map is scrollable vertically
    - EventView shows event text and choice buttons for each player
    - CampfireView shows rest/smith options (with relic-modified options)
    - TreasureView shows relic gained
    - MerchantView shows cards, relics, potions with prices and buy buttons
    - Map toggle button accessible during combat
  artifacts:
    - packages/client/src/components/map/MapView.tsx
    - packages/client/src/components/rooms/EventView.tsx
    - packages/client/src/components/rooms/CampfireView.tsx
    - packages/client/src/components/rooms/MerchantView.tsx
  key_links:
    - MapView reads map data from GameState
    - Room views read room-specific state from GameState
    - All user actions send ClientMessage through websocket
---

<objective>
Create the map UI and non-combat room view components.

Purpose: Players need to see the Act 1 map, navigate between rooms, and interact with non-combat rooms (events, campfires, treasure, merchant).
Output: MapView with node rendering and boot meeple; EventView, CampfireView, TreasureView, MerchantView components.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-flow/05-RESEARCH.md
@.planning/phases/05-game-flow/05-01-SUMMARY.md
@.planning/phases/05-game-flow/05-03-SUMMARY.md
@packages/client/src/styles/variables.module.css

<interfaces>
From packages/client/src/stores/gameStore.ts:
```typescript
export type AppPhase = 'connecting' | 'lobby' | 'combat';
export interface AppState {
  playerId: string | null;
  game: GameState | null;
  phase: AppPhase;
  // ...
}
```

GameState (after Plan 01):
```typescript
GameState += {
  gamePhase: 'MAP' | 'EVENT' | 'CAMPFIRE' | 'TREASURE' | 'MERCHANT' | 'COMBAT' | 'REWARDS' | 'BOSS_REWARD',
  map: { nodes: MapNode[], bossNodeId: string, currentNodeId: string | null },
  currentFloor: number,
  eventState: { eventId: string, playerChoices: Record<string, number | null> },
  merchantState: { cardPool, relicPool, potionPool, removeCost, playersRemoved },
  campfireState: { playerChoices: Record<string, string | null> },
}
```

From packages/shared/src/data/events.ts:
```typescript
export const eventMap: Record<string, EventCard>;
```

Client send function (from websocket service):
```typescript
connection.send({ type: 'SELECT_NODE', nodeId: '2-1' });
connection.send({ type: 'EVENT_CHOICE', choiceIndex: 0 });
// etc.
```

Existing CSS module patterns from Phase 4 (dark fantasy theme):
- CSS custom properties: --bg-dark, --bg-card, --text-primary, --accent-gold, --border-subtle, etc.
- Card-like containers with box-shadow and border-radius
- Responsive grid layouts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapView and MapNode components</name>
  <files>packages/client/src/components/map/MapView.tsx, packages/client/src/components/map/MapView.module.css, packages/client/src/components/map/MapNode.tsx, packages/client/src/components/map/MapNode.module.css, packages/client/src/stores/gameStore.ts</files>
  <action>
1. Update `gameStore.ts`:
   - Change `AppPhase` to `'connecting' | 'lobby' | 'game'` (replace 'combat' with 'game' since the game now has multiple phases)
   - In `handleServerMessage` STATE_UPDATE handler, set phase to `'game'` instead of `'combat'`
   - Add a `mapVisible` boolean to AppState for the map toggle (default false)
   - Add `toggleMap()` method to AppStore

2. Create `MapNode.tsx`:
   - Props: `node: MapNode`, `isCurrent: boolean`, `isAvailable: boolean`, `isHost: boolean`, `onClick: () => void`
   - Render a circular node with an icon based on room type:
     - encounter: crossed swords icon (unicode or SVG)
     - elite: skull icon
     - event: question mark
     - campfire: flame icon
     - treasure: chest icon
     - merchant: dollar/coin icon
     - boss: crown icon
   - Use CSS classes for states: `.current` (highlighted border + boot meeple below), `.available` (bright, clickable, host-only), `.visited` (dimmed), `.unavailable` (grayed out)
   - The boot meeple is a simple styled div/span below the current node (a boot emoji or small icon)
   - Only host can click available nodes; non-host sees cursor: default

3. Create `MapView.tsx`:
   - Reads `state.game.map` from the game store
   - Renders 15 floors vertically, floor 14 (boss) at top, floor 0 at bottom
   - Each floor renders its nodes horizontally centered
   - Connection lines drawn between nodes using CSS borders or SVG lines connecting parent to child nodes
   - Current floor and available next nodes are highlighted
   - Container is scrollable (overflow-y: auto) with auto-scroll to current position
   - When the map toggle is active during combat, render as an overlay (read-only, no click handlers)

4. CSS styling:
   - Dark background matching the game theme
   - Nodes are circular (40-50px) with themed borders
   - Connection lines are subtle (--border-subtle color)
   - Current node has a glowing accent border (--accent-gold)
   - Available nodes pulse or have a brighter border
   - Floor labels on the left (Floor 1, 2, ... 15)
   - Vertical spacing between floors for readability
   - The map is narrow (max-width: 400px) centered on screen
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MapView renders 15-floor vertical map with room type icons. Boot meeple shows current position. Host can click available nodes. Map scrolls vertically. Dark fantasy theme styling applied.</done>
</task>

<task type="auto">
  <name>Task 2: Non-combat room views (Event, Campfire, Treasure, Merchant)</name>
  <files>packages/client/src/components/rooms/EventView.tsx, packages/client/src/components/rooms/EventView.module.css, packages/client/src/components/rooms/CampfireView.tsx, packages/client/src/components/rooms/CampfireView.module.css, packages/client/src/components/rooms/TreasureView.tsx, packages/client/src/components/rooms/TreasureView.module.css, packages/client/src/components/rooms/MerchantView.tsx, packages/client/src/components/rooms/MerchantView.module.css</files>
  <action>
**EventView.tsx**:
- Reads `state.game.eventState` to get the current event
- Looks up event data from `eventMap` using eventId
- Displays event name, text/flavor, and choice buttons
- Each player sees their own choice buttons; once chosen, shows "Waiting for others..."
- Shows which players have chosen (checkmarks next to names)
- Sends `EVENT_CHOICE` message on button click

**CampfireView.tsx**:
- Displays campfire scene with choice buttons: Rest, Smith
- Additional buttons if player has relevant relics: Dig (Shovel), Lift (Girya), Toke (Peace Pipe)
- Grayed out options if blocked by relics (Coffee Dripper blocks Rest, Fusion Hammer blocks Smith)
- Rest shows "Heal 3 HP" (+ Regal Pillow bonus if applicable)
- Smith shows card selection for upgrade (list player's deck cards, click to select)
- Sends `CAMPFIRE_CHOICE` message with choice and optional cardId

**TreasureView.tsx**:
- Shows the relic(s) gained with relic name and text
- Simple display with "Continue" button (treasure resolves instantly on server)
- Dark fantasy treasure chest visual

**MerchantView.tsx**:
- Three sections: Cards for Sale, Relics, Potions
- Each item shows name, text/description, and gold price
- Buy button per item (grayed out if insufficient gold)
- Player's gold displayed prominently
- "Remove a Card" section at bottom with cost displayed
- If player already removed a card this visit, section is grayed out
- Card removal shows player's deck cards to select from
- "Leave" button to exit merchant
- Sends MERCHANT_BUY, MERCHANT_REMOVE_CARD, or MERCHANT_LEAVE messages

**CSS patterns for all views**:
- Same dark fantasy theme as combat UI
- Card-like containers for items
- Gold values in --accent-gold color
- Disabled state for unavailable options
- Consistent padding and spacing with combat components
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 4 non-combat room views render correctly with dark fantasy styling. Event choices, campfire options, merchant buying, and treasure display all functional. All views send appropriate client messages.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with all new components
2. MapView renders vertical map with icons and connections
3. All room views handle their specific interactions
4. CSS follows dark fantasy theme from Phase 4
5. Map toggle works during combat (read-only overlay)
</verification>

<success_criteria>
Players see the Act 1 map with room type icons. Host navigates by clicking nodes. Boot meeple marks current position. All non-combat room views (Event, Campfire, Treasure, Merchant) present appropriate choices and send correct messages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-flow/05-06-SUMMARY.md`
</output>
