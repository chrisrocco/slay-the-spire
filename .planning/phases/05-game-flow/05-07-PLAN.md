---
phase: 05-game-flow
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-04, 05-05]
files_modified:
  - packages/client/src/components/rewards/RewardView.tsx
  - packages/client/src/components/rewards/RewardView.module.css
  - packages/client/src/components/rewards/BossRelicView.tsx
  - packages/client/src/components/rewards/BossRelicView.module.css
  - packages/client/src/components/combat/PotionSlots.tsx
  - packages/client/src/components/combat/PotionSlots.module.css
  - packages/client/src/app.tsx
autonomous: true
requirements: [RWRD-01, RWRD-02, RWRD-03, RWRD-04, RWRD-05, RWRD-06, ITEM-03, ITEM-04]
must_haves:
  truths:
    - RewardView shows all available rewards (gold, cards, potion, relic) before player makes choices
    - Card reward shows 3 cards with pick/skip options
    - Potion reward shows potion with take/skip; grayed if at limit
    - Relic reward shows relic with take option
    - Gold is displayed and auto-collected
    - BossRelicView shows (playerCount + 1) boss relics for selection
    - PotionSlots show current potions with use/discard/pass buttons
    - App.tsx routes to correct view based on gamePhase
    - Map toggle button visible during combat
  artifacts:
    - packages/client/src/components/rewards/RewardView.tsx
    - packages/client/src/components/rewards/BossRelicView.tsx
    - packages/client/src/components/combat/PotionSlots.tsx
    - packages/client/src/app.tsx
  key_links:
    - app.tsx is the top-level view router
    - RewardView reads rewardState from GameState
    - PotionSlots integrates into PlayerBoard
---

<objective>
Create reward UI, potion management UI, and wire the app router to handle all game phases.

Purpose: Players need to see and pick rewards after combat, manage potions, and the app needs to route between map/combat/room views based on gamePhase.
Output: RewardView, BossRelicView, PotionSlots components; updated app.tsx routing.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-flow/05-RESEARCH.md
@.planning/phases/05-game-flow/05-01-SUMMARY.md
@.planning/phases/05-game-flow/05-04-SUMMARY.md
@.planning/phases/05-game-flow/05-05-SUMMARY.md

<interfaces>
From packages/client/src/app.tsx:
```typescript
export function App() {
  // Currently routes: connecting -> lobby -> combat (CombatView)
  // Needs to handle: MAP, EVENT, CAMPFIRE, TREASURE, MERCHANT, COMBAT, REWARDS, BOSS_REWARD
}
```

RewardState (from Plan 01):
```typescript
{
  gold: number,
  cardRewards: { cardIds: string[], upgraded: boolean }[],
  potionReward: string | null,
  relicReward: string | null,
  playerChoices: Record<string, { cardPicked, potionPicked, relicPicked, skipped }>,
}
```

From packages/shared/src/data/relics.ts:
```typescript
export const relicMap: Record<string, RelicCard>;
```

From packages/shared/src/data/potions.ts:
```typescript
export const potionMap: Record<string, PotionCard>;
```

Client send function:
```typescript
send({ type: 'REWARD_PICK_CARD', cardId: 'bash' });
send({ type: 'REWARD_PICK_POTION' });
send({ type: 'REWARD_PICK_RELIC' });
send({ type: 'REWARD_SKIP' });
send({ type: 'PASS_POTION', potionId: 'fire_potion', targetPlayerId: 'player2' });
send({ type: 'DISCARD_POTION', potionId: 'fire_potion' });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RewardView and BossRelicView components</name>
  <files>packages/client/src/components/rewards/RewardView.tsx, packages/client/src/components/rewards/RewardView.module.css, packages/client/src/components/rewards/BossRelicView.tsx, packages/client/src/components/rewards/BossRelicView.module.css</files>
  <action>
**RewardView.tsx**:
- Displays after combat in REWARDS gamePhase
- Shows all rewards available to the current player in a list:
  1. **Gold**: Amount auto-collected (display: "+{amount} Gold")
  2. **Card Reward**: Show 3 card options (using Card component from combat). Player clicks one to pick, or clicks "Skip" button. Cards use card lookup for name/text display.
  3. **Potion Reward**: Show potion name and text. "Take" button (disabled if at limit with tooltip "Potion slots full"). "Skip" button.
  4. **Relic Reward**: Show relic name and text. "Take" button.
- After player makes all choices (or skips), show "Waiting for other players..." if others haven't finished
- Display other players' pick status (checkmark or hourglass)
- Sends REWARD_PICK_CARD, REWARD_PICK_POTION, REWARD_PICK_RELIC, or REWARD_SKIP messages

**BossRelicView.tsx**:
- Displayed in BOSS_REWARD gamePhase after boss combat
- Shows (playerCount + 1) boss relic cards in a row
- Each player selects one. Once selected, it's marked as taken (grayed out for others)
- Display relic name, text, and any downside clearly
- Boss relics that replace starter relics show "Replaces: [starter relic name]"
- After all players pick, transitions automatically

**CSS styling**:
- Reward container centered, card-like background
- Card reward uses existing Card component styling
- Gold displayed with --accent-gold color and coin icon
- Relic/potion items in styled containers with name bold, text below
- Take/Skip buttons styled consistently with combat EndTurnButton
- Disabled states for full potion slots
- Smooth transitions for pick/reveal animations (simple opacity/transform)
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>RewardView shows all rewards before player picks. Card/potion/relic picks send correct messages. BossRelicView handles boss relic selection. Styling matches dark fantasy theme.</done>
</task>

<task type="auto">
  <name>Task 2: PotionSlots component and app routing</name>
  <files>packages/client/src/components/combat/PotionSlots.tsx, packages/client/src/components/combat/PotionSlots.module.css, packages/client/src/app.tsx</files>
  <action>
**PotionSlots.tsx**:
- Shows 3 potion slots (5 with Potion Belt) in the PlayerBoard area
- Each occupied slot shows potion name with a small icon
- Click potion to expand action menu:
  - During combat: "Use" button (sends USE_POTION with optional target selection for targeted potions)
  - Outside combat (MAP phase): "Pass" button (shows player list to select target, sends PASS_POTION)
  - "Discard" button always available (sends DISCARD_POTION)
- Empty slots show as faded outlines
- Tooltip on hover shows full potion text
- Integrate into PlayerBoard.tsx or PlayerStats.tsx layout

**App.tsx routing update**:
- Replace the simple `phase === 'combat'` check with game-phase-aware routing:
  ```
  when gamePhase === 'MAP' -> <MapView />
  when gamePhase === 'COMBAT' -> <CombatView /> (existing)
  when gamePhase === 'EVENT' -> <EventView />
  when gamePhase === 'CAMPFIRE' -> <CampfireView />
  when gamePhase === 'TREASURE' -> <TreasureView />
  when gamePhase === 'MERCHANT' -> <MerchantView />
  when gamePhase === 'REWARDS' -> <RewardView />
  when gamePhase === 'BOSS_REWARD' -> <BossRelicView />
  ```
- Add a map toggle button (floating, top-right corner) that overlays MapView during combat (read-only)
- Import all new components
- Pass `state`, `send`, and `onReturnToLobby` props as needed to each view
- The app uses `state.game?.gamePhase` to determine which view to show
- During 'COMBAT', also pass PotionSlots to CombatView (or integrate PotionSlots into PlayerBoard)
  </action>
  <verify>
    <automated>cd /home/chris/projects/slay-the-spire && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PotionSlots component integrated with use/pass/discard actions. App.tsx routes to all game phase views. Map toggle works as overlay during combat. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles
2. App routes to correct view for each gamePhase
3. RewardView displays all rewards before player picks
4. BossRelicView handles boss relic selection
5. PotionSlots show potion management options appropriate to game phase
6. Map toggle works during combat
</verification>

<success_criteria>
Players see rewards after combat with pick/skip options. Boss relic selection works. Potion slots show in combat with use/pass/discard options. App correctly routes between all game phases. Map toggle accessible during combat.
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-flow/05-07-SUMMARY.md`
</output>
